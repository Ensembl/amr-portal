datasets:
[
    "phenotype":{
        table:"",
        columns:[
            id:"",
            label:"",
            type:"",
            is_sortable:false
        ]
    }
]

## TODO 

- input to parquet
    - join columns
    - filter rows
- update filters
- load
    - load in genotype
    - load in phenotype
    - load in datasets
    - load filters
        - category
        - views
        - mapping
        - column order
- use
    - look at generating payloads
    - 